# Local-only Redis port binding. The app port is published by the base Compose
# file because the deployed reverse proxy reaches the host on port 8000.
services:
  redis:
    ports:
      - "6379:6379"
