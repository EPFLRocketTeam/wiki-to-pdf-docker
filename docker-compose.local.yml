# Local-only host port bindings. Production reaches the app through the external
# nginx-proxy-manager Docker network declared in the base compose file.
services:
  web-go:
    ports:
      - "8000:8000"

  redis:
    ports:
      - "6379:6379"
