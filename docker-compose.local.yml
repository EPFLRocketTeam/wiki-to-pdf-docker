# Local-only Redis port binding. The application binds to 127.0.0.1:8000 in the
# base compose file so the host reverse proxy and local browser can reach it.
services:
  redis:
    ports:
      - "6379:6379"
