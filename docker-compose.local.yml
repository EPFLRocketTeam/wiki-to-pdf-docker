# Local-only host port bindings. Dokploy deploys docker-compose.yml without this
# file and routes to the service's internal exposed port.
services:
  web-go:
    ports:
      - "8000:8000"

  redis:
    ports:
      - "6379:6379"
