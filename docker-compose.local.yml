# Local-only host port bindings. Dokploy routes production traffic to the
# service's internal exposed port without requiring a named external network.
services:
  web-go:
    ports:
      - "8000:8000"

  redis:
    ports:
      - "6379:6379"
